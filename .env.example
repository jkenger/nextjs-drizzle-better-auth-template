# Database
DATABASE_URL=postgresql://user:password@localhost:5432/vault

# Better Auth
BETTER_AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000
# Optional: For client-side auth (auto-detects from window.location.origin if not set)
# NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Email Configuration (for password reset and verification)
# Option 1: Resend (recommended) - https://resend.com
# RESEND_API_KEY=re_xxxxxxxxxxxxx

# Option 2: SMTP (any provider)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com
